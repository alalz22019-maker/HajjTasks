{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /home/user/MyTasks && npm run build 2>/dev/null && git add -f dist/ 2>/dev/null; git diff --cached --quiet || git commit -m 'build: تحديث تلقائي بعد التعديل' && git push github deploy-setup:main 2>/dev/null || true",
            "timeout": 120,
            "statusMessage": "نشر التحديثات على Vercel..."
          }
        ]
      }
    ]
  }
}
